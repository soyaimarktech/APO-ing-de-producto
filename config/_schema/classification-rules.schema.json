{
  "_draft": "JSON Schema draft-07",
  "_id": "apo/classification-rules.schema.json",
  "title": "APO · classification-rules.json",
  "type": "object",
  "required": ["schemaVersion", "rules"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "_nota": { "type": "string" },
    "matchOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "caseInsensitive": { "type": "boolean" },
        "accentInsensitive": { "type": "boolean" }
      }
    },
    "rules": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "match", "destination", "priority"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "description": { "type": "string" },
          "match": {
            "type": "object",
            "required": ["type"],
            "additionalProperties": false,
            "properties": {
              "type": { "type": "string", "enum": ["glob", "regex", "any", "none"] },
              "patterns": { "type": "array", "items": { "type": "string" } }
            }
          },
          "destination": { "type": ["string", "null"] },
          "priority": { "type": "integer", "minimum": 0 }
        }
      }
    }
  }
}
